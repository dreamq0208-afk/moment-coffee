# 此刻咖啡馆 · 开发说明

这份文件写给在本项目里干活的 Codex。开工前先读完它。

## 这是什么

一个手机网页 demo。用户点几下说出此刻的状态，我们推荐一杯真能点到的咖啡，再给一段咖啡师手写的留言和一张能分享的小票。

一句话：用一杯咖啡，接住你此刻的心情。

## 先读这些

1. `docs/product-spec.md`：功能、流程、推荐规则、咖啡和选项数据。
2. `docs/design-spec.md`：颜色、字体、动效、交互细节。**好看和手感是这个项目的命根子**，改 UI 前必读。
3. `docs/copy-prompt.md`：生成杯套留言的提示词和写作规则。
4. `prototype/index.html`：已经能跑的单文件原型。行为以它为准。

文档和原型对不上时，以文档为准，并在交付时说明。

## 现在的状态

原型已经跑通整条流程：点单、推荐、倒咖啡动画、杯套留言、改一下、换一杯、重新出杯、小票。

原型是在 claude.ai 里做的，有两处依赖那边的环境：

- 留言用 `window.claude.use("sample")` 调 AI。本地没有这个对象，会自动走本地模板留言。
- 保存小票用 `window.claude.use("downloads")`。本地没有，按钮会被隐藏。

这两处是本轮要替换掉的。

## 本轮任务

按顺序做。每做完一项，跑一遍自测再往下。

### 任务 1：拆成正常的前端工程

- 用 Vite + 原生 JavaScript（不用框架）。这个项目状态不多，框架带来的负担大于好处。
- 按职责拆文件，建议：

```
src/
  main.js        页面切换和整体流程
  data.js        咖啡、感受、修改选项、推荐分数
  recommend.js   根据电量、感受、时间算推荐
  cup.js         杯子绘制、倒咖啡动画、改动时的过渡
  edit.js        修改页：标签、选项、心情记录
  copy.js        留言：调接口，失败时用模板
  receipt.js     小票渲染和保存图片
  styles.css
```

- **验收**：拆完后，所有交互和原型一模一样。截图对比点单页、结果页、修改页、改完后的结果页。

### 任务 2：接入真实的 AI 留言

- 加一个本地后端接口 `POST /api/copy`。可以用 Vite 的 dev server 中间件，也可以单独起一个很小的 Node 服务。
- API Key 放 `.env`，**绝不能出现在前端代码里**。`.env` 写进 `.gitignore`，再提供一份 `.env.example`。
- 模型服务商先留成可配置的，具体用哪家等我定。
- 前端 `copy.js` 只认这个接口：

```
请求：{ order, drink, moods }
返回：{ name, why, note: ["...", "..."] }
```

- 超过 15 秒没返回、报错、或返回格式不对，都用本地模板兜底。用户永远能拿到一杯完整的咖啡。
- 提示词照搬 `docs/copy-prompt.md`，不要自己改写规则。
- **验收**：断网时流程不断；正常时留言会提到用户的具体选择；改过配方后，留言里不出现"换成""改成""原本"这类字。

### 任务 3：保存小票

- 用 html2canvas 把小票截成 PNG，用 `<a download>` 触发下载。
- 手机上下载体验不好的话，改成弹出图片让用户长按保存。
- **验收**：iPhone Safari 和安卓 Chrome 上都能拿到图片。小票上下的锯齿边在图片里要保留。

### 任务 4：字体本地化

- 原型用的是 Google Fonts。**国内打不开**，而我们的用户在国内。
- 改成本地字体文件，并按页面实际用到的字做子集化，控制体积。
- 手写体（杯套、修改页那行字）是氛围关键，加载失败时要有体面的降级，不能闪成宋体。
- **验收**：断开外网后刷新，字体正常。首屏字体总体积尽量控制在 500KB 以内。

### 任务 5：咖啡介绍

- 结果页咖啡名字下面加一个很轻的入口「这杯是什么」。
- 点开显示口感、咖啡因档位、适合什么时候喝。数据在 `docs/product-spec.md` 第五节。
- 用小抽屉或原地展开，别跳页面。

## 做事的规矩

- **不要加功能**。本轮只做上面五项。有好点子写进交付说明，别直接做。
- **不要改文案语气**。所有界面文字都要短、口语、不说教。新增文字前先看 `docs/design-spec.md` 的文案一节。
- **不要动推荐规则和留言规则**，除非任务里明确写了。
- 改 UI 之后必须在 390×844 的手机尺寸下截图自查，确认没有错位、没有横向滚动。
- 支持深色模式。系统开了"减少动态效果"时，关掉动画直接出结果。

## 交付时告诉我

- 做了什么，没做什么，为什么。
- 怎么在本地跑起来（命令写全）。
- 发现的问题和建议，单独列出来。
